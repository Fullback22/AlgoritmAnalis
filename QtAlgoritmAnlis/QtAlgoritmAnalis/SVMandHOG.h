#pragma once
#include <opencv2/core/core.hpp>
#include <opencv2/highgui/highgui.hpp>
#include <opencv2/opencv.hpp>
//#include <opencv/ml.h>
#include "iostream"
#include <fstream>
#include <math.h>

using namespace std;
using namespace cv;

class SVMandHOG
{
	/*vector<Rect> detectObj;
	vector<Rect> detectObjFilter;
	SVM my_SVM;
	vector<Mat> out_images;
	HOGDescriptor hog;
	int medianBlurSize;
	int gausianBlurSize;
	int sizeRect;
public:
	SVMandHOG();
	SVMandHOG(string fileName);
	~SVMandHOG();
	void kaskad(Mat &in_images, int step, int size_rect, int size_hog);
	void kaskad_SVM(Mat &in_images);
	void kaskad(Mat &in_images);
	void draw(Mat &inOutputImg);
	int getDetectedObj();
	int getObjX(int N);
	int getObjY(int N);
	int getObjHigth(int N);
	int getObjWigth(int N);
	void setParams(int medianBlurSize, int gausianBlurSize, int sizeRect);*/
};

